## 1. 如何搭建项目  
安装 node.js → 使用 vite 构建 vue 项目

## 2. 构思页面
首步分解任务：

将页面拆分成：
1. 用户身份与注册
2. 主页内容与卡片
3. 上传图片与分析
4. 前后端交互

## 3. 然后我让 AI 帮忙写了用户注册和登录的页面
发现输入密码时，他的小眼睛是一次性的（待解决）。

## 4. 应用状态管理（前端核心机制之一）
在一个前端应用中，用一个中央数据来记录用户当前所在的位置、是否已登录等信息，这个中央数据就叫做“状态”，当状态改变时页面会重新渲染。

## 5. 组件事件
子组件通过定义和调用事件来通知父组件发生了什么事。

## 6. 属性（Props）
Props 是 Vue 中组件间通讯最基本的方式，用于父组件向子组件传递数据。

## 7. 资料完善页面的“稍后填写”功能还没实现（待解决）

## 8. 页面无法完全填充屏幕
结果发现是因为创建 vue 项目时自带的 css/style 文件里的东西没有删除干净，导致它控制了页面布局的 style，所以即使在 App.vue 修改也会被覆盖。
